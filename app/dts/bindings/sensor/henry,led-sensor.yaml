description: LED controlled through the Zephyr sensor API

compatible: "henry,led-sensor"

include: base.yaml

properties:
  led:
    type: phandle
    required: true
    description: GPIO LED node controlled by this driver
