config HENRY_LED_SENSOR
	bool "Henry LED sensor driver"
	depends on SENSOR && GPIO
	default y if DT_HAS_HENRY_LED_SENSOR_ENABLED
	help
	  Expose a GPIO LED through sensor_sample_fetch and sensor_channel_get.
