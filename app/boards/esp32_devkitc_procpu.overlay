/ {
	leds {
		compatible = "gpio-leds";

		led0: led_0 {
			gpios = <&gpio0 2 GPIO_ACTIVE_HIGH>;
			label = "External heartbeat LED on GPIO2";
		};
	};

	aliases {
		led0 = &led0;
	};
};

#include "../app.overlay"
